const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { console.log('PAYMENTS:', await prisma.payment.findMany({ include: { allocations: true }, orderBy: { createdAt: 'desc' }, take: 5 })); console.log('PURCHASES:', await prisma.purchase.findMany({ orderBy: { createdAt: 'desc' }, take: 5 })); } main().finally(() => prisma.$disconnect());
